% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figure_info.R
\name{figure_info}
\alias{figure_info}
\title{Add figure information as a caption to a ggplot}
\usage{
figure_info(
  custom_text = NULL,
  include_r_version = TRUE,
  datetime_format = "\%d.\%m.\%Y \%X"
)
}
\arguments{
\item{custom_text}{Optional. A string of custom text to include at the beginning of the caption.}

\item{include_r_version}{Optional. A logical indicating whether to include R version information in the caption. Defaults to TRUE.}

\item{datetime_format}{Optional. A string defining the date and time format for the caption. Defaults to "\%d.\%m.\%Y \%X".}
}
\value{
A \code{figure_info} object that can be added to a ggplot object.
}
\description{
This function adds metadata to a ggplot as a caption, including the current date,
script name, and optionally the R version.
}
\examples{
library(ggplot2)
library(gecko.utils)

# Create a simple ggplot with r-version and datetime and R script name
ggplot(mtcars, aes(x = wt, y = mpg)) +
geom_point() +
figure_info()

# Create a simple ggplot with additional custom text
ggplot(mtcars, aes(x = wt, y = mpg)) +
geom_point() +
figure_info(custom_text = "Data source: mtcars")

}
